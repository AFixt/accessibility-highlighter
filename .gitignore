/.idea
/node_modules
/coverage
/dist
/reports
/scratchpads
*.zip
*.log
.DS_Store
.claude

# Artifacts written into the repo root by CI steps (ci.yml, security.yml).
# Ignored so a local reproduction of those steps can't be committed by accident.
audit.json
audit.txt
audit-body.md
npm-audit-results.json
licenses.json

# Playwright output
/playwright-report
/test-results

# lychee's on-disk request cache (npm run links)
.lycheecache

# Local-only working notes
errors.md
element-highlighting-guide.md

# Ad-hoc editor backups. Git is the backup; 26 of these (12,949 lines) were
# tracked until #127, invisible to Jest, ESLint, Knip and jscpd because each
# correctly skips them, so nothing could ever report them.
*.bak
*.bak2
